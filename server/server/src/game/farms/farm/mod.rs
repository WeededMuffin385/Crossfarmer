mod Plant;

const WIDTH: usize = 16;
const LENGTH: usize = 16;

/*
struct Farm {
    mail: String,
    data: Vec<Plant>,
}

 */

/*
impl Farm {
    pub fn load(mail: String) -> Self {
        Self {
            mail,
            data: vec![Default::default();WIDTH * LENGTH]
        }
    }
}

 */